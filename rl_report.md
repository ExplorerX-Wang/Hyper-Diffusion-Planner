# 七篇强化学习文章的方法与范式对比分析

> 分析时间：2026-07-23

---

## 一、论文概览与分类

| 论文 | 年份 | 应用领域 | 核心RL方法 | 大类 |
|------|------|----------|------------|------|
| **CarPlanner** | 2025 | 端到端轨迹规划 | PPO + 自回归模型 | **纯RL** |
| **DiffusionQL** | 2023 | 通用离线RL | Q-learning + 扩散策略 | **纯RL**（离线） |
| **DPPO** | 2024 | 机器人控制 | PPO + 扩散去噪MDP | IL + RL |
| **DiffusionDriveV2** | 2025 | 端到端自动驾驶 | GRPO + 锚点截断扩散 | IL + RL |
| **DriveDPO** | 2025 | 端到端自动驾驶 | DPO + 锚点分类策略 | IL + RL |
| **DIPOLE** | 2026 | 通用RL + 自动驾驶 | 二分扩散策略优化 | IL + RL |
| **HDP** | 2026 | 端到端自动驾驶(实车) | KL正则化加权回归 | IL + RL |

---

## 二、纯RL（无IL预训练，RL从头训练或联合训练）

该类别的方法**不依赖单独的IL预训练阶段**，策略完全由RL过程驱动。根据是否有环境交互，进一步分为**在线RL**和**离线RL**两子类。

### 2.1 在线RL：CarPlanner — Model-based PPO

CarPlanner 是6篇中唯一采用**经典在线RL**路线的工作——不需要任何IL预训练，RL从零开始训练。

#### 核心设计

- **MDP建模**：将轨迹规划建模为多步序列决策，动作 $a_t$ 定义为自车下一帧位姿
- **世界模型**：非反应式转移模型（预测其他agents未来轨迹），使训练可在GPU上高效执行，时间成本与IL方法相当
- **自回归生成**：policy是单步Gaussian分布，自回归式逐帧生成轨迹

#### 关键创新

1. **一致性模式表示**（Consistent Mode Representation）
   - 纵向-横向分解：纵向 = 平均速度标量，横向 = 来自地图+自车状态的所有可行路线
   - Mode在时间步之间保持恒定，为自回归策略提供稳定引导
   - 解决了vanilla自回归模型"随机采样导致跨步不一致"的问题

2. **生成-选择框架**
   - Mode Selector：输出各mode的分数 → Trajectory Generator：自回归条件生成 → Rule-Augmented Selector：安全/舒适/进度补偿
   
3. **专家引导奖励 + 任务导向奖励**
   - $r = r_{expert}$（与专家轨迹的位移误差）$+ r_{task}$（碰撞、可行驶区域、进度）
   - 通用奖励设计，无需场景特定调参

4. **IVM**（Invariant-View Module）
   - 将agent、地图、路线信息统一转换到自车当前坐标系
   - KNN裁剪远处无关信息，提供时间不变的特征

#### 评价

- 数据集：nuPlan（1200小时真实驾驶数据），仿真闭环评估
- 首个在nuPlan上超越SOTA IL和基于规则方法的RL规划器

---

### 2.2 离线RL：DiffusionQL — Q-learning + 扩散策略（联合训练）

DiffusionQL 将**行为克隆和Q-learning融合为端到端的单一训练过程**，从静态数据集学习，无需在线交互。

#### 核心设计

- 用条件扩散模型表达策略 $\pi_\theta(a|s)$，替代传统Gaussian策略
- 训练目标 = 扩散行为克隆损失 + Q值最大化引导：

$$L(\theta) = \underbrace{\mathbb{E}_{t, \epsilon, s, a}\left[||\epsilon - \epsilon_\theta||^2\right]}_{L_d\text{: 行为克隆}} - \underbrace{\alpha \cdot \mathbb{E}_{s, a^0}\left[Q_\phi(s, a^0)\right]}_{L_q\text{: Q值引导}}$$

- Q值梯度通过**完整扩散反向链**回传到扩散模型参数（从 $a^0$ 经 $a^1, ..., a^T$）

#### 关键创新

1. **扩散策略的表达力优势**
   - Gaussian策略在多模态行为数据上出现mode-covering（取均值，导致OOD动作）
   - 扩散模型天然捕捉多模态分布，精准复现数据中的多个行为模式
   
2. **自适应Q值归一化**
   - $\alpha = \eta / \mathbb{E}[|Q_\phi(s, a)|]$，防止Q项主导训练

#### 评价

- 数据集：D4RL（MuJoCo locomotion），纯仿真基准
- 核心贡献：证明了扩散模型作为策略参数化在离线RL中的表达力优势
- 局限：Q值梯度需通过完整扩散链，计算成本高；纯离线，无在线改进能力

---

## 三、IL + RL（IL预训练 → RL微调/后训练）

该类别的方法采用**两阶段训练范式**：第一阶段用IL（行为克隆/模仿学习）获得强大的行为先验，第二阶段用RL针对性优化。根据RL阶段的优化方式，分为**三个子类**：策略梯度微调（梯度通过扩散链/离散策略logit）、偏好对齐微调（DPO）和加权回归微调（梯度不通过扩散链）。

### 3.1 策略梯度微调：DPPO 与 DiffusionDriveV2

DPPO 和 DiffusionDriveV2 同属策略梯度路线，核心共性是将**扩散去噪过程视为MDP**，在其上做策略梯度更新。**DiffusionDriveV2 在方法论上继承自 DPPO**：保留"去噪MDP + 策略梯度"的框架，但在策略参数化、优势估计和探索方式上做了三处关键改进，使其更适合大规模驾驶任务。

#### 共同基础：去噪MDP + 策略梯度

两篇工作共享同一个核心思想——将扩散模型的迭代去噪过程展开为MDP：

- **外层**：环境MDP，$a$ 是干净动作
- **内层**（扩散去噪MDP）：$(a_k, a_{k-1}, p_\theta, s)$，从噪声 $a_K$ 逐步去噪到 $a_0$
- 每个去噪步骤 $p_\theta(a_{k-1}|a_k, s)$ 是Gaussian，有可计算log-likelihood
- 在去噪链上做策略梯度更新（PG/PPO/GRPO），梯度流经整个或部分去噪链

---

#### DPPO 的设计：标准扩散 + PPO

**策略形式**：标准DDPM扩散策略，从纯噪声无条件生成动作。

**两阶段流程**：
- 阶段1（IL）：从演示数据训练扩散策略（DDPM behavior cloning）
- 阶段2（RL）：PPO在线微调扩散策略

**关键设计**：

1. **部分去噪微调**：只微调最后 $K_{grad}$ 步去噪（避免梯度爆炸/消失，减少计算量）
2. **噪声schedule裁剪**：$[\sigma_{min}^{exp}, \sigma_{max}]$ 控制探索程度
3. **Likelihood裁剪**：$\sigma_{min}^{prob}=0.1$，防止Gaussian方差过小导致数值不稳定
4. **DDIM采样**：推理时DDIM加速，训练时DDPM模式增加随机性

**优势**：方法论上有完备的MDP理论和PG推导，通用性强。

**局限**：标准扩散搜索空间大（从纯噪声出发），PPO需价值网络，计算开销高（需存储去噪步梯度）。

---

#### DiffusionDriveV2 的改进：锚点扩散 + GRPO

DiffusionDriveV2 继承了 DPPO 的"去噪MDP + 策略梯度"框架，但在三个关键维度做了改进。

**策略形式**：锚点截断扩散——用K-Means聚类人类轨迹得到N个锚点，扩散模型只建模锚点附近的残差。

**两阶段流程**：
- 阶段1（IL）：训练DiffusionDrive（锚点扩散模型），GMM先验
- 阶段2（RL）：GRPO微调，IL损失作为正则化项

**三个关键改进**（相对DPPO）：

| 改进维度 | DPPO | DiffusionDriveV2 | 改进动机 |
|----------|------|-------------------|----------|
| **策略参数化** | 标准扩散（纯噪声→动作） | 锚点截断扩散（锚点+残差） | 大幅缩小搜索空间，锚点提供意图先验 |
| **优势估计** | PPO + GAE（需价值网络） | GRPO（组内相对比较） | 去掉价值网络，更轻量；组内比较隔离不同意图 |
| **探索噪声** | 加性Gaussian噪声 | 乘性噪声（仅2维：纵向+横向） | 保持轨迹平滑性，更适合驾驶场景 |
| **优势裁剪** | PPO clip（对称裁剪±ε） | 非对称截断：正优势保留，负优势置0 | 只奖励相对好的，不惩罚安全轨迹 |

**双层GRPO设计**（相比DPPO单层PPO的核心创新）：

- **Intra-Anchor GRPO**：每个锚点内部做组相对优势
  $$A_i = \frac{r_i - \text{mean}(\{r_j\}_{j \in \text{anchor}_i})}{\text{std}(\{r_j\}_{j \in \text{anchor}_i})}$$
  直接复现了DPPO的"扩散MDP中的策略梯度"思路，但用GRPO替代PPO，同时不同锚点间隔离避免mode collapse。

- **Inter-Anchor Truncated GRPO**：跨锚点全局视角，负优势截断为0，碰撞轨迹强制-1。

**继承关系总结**：

```
DPPO（2024，机器人）
  │
  │  核心框架：去噪MDP + 策略梯度
  │
  └──→ DiffusionDriveV2（2025，自动驾驶）
        ├── 保留：去噪链上做PG、IL预训练→RL微调
        ├── 改进1：标准扩散 → 锚点截断扩散（减小搜索空间）
        ├── 改进2：PPO+GAE → GRPO（去掉价值网络）
        └── 改进3：加性噪声 → 乘性噪声（轨迹平滑性）
```

#### DPPO vs DiffusionDriveV2 对比

| 维度 | DPPO | DiffusionDriveV2 |
|------|------|-------------------|
| **RL算法** | PPO + GAE | GRPO（Intra + Inter两层） |
| **是否需价值网络** | 是（Critic网络） | 否（组内比较） |
| **策略类型** | 标准扩散（DDPM/DDIM） | 锚点截断扩散（GMM先验 + 残差扩散） |
| **动作空间** | 动作块（action chunk） | 完整轨迹 |
| **探索方式** | 加性噪声 + schedule裁剪 | 乘性噪声（2维：纵向+横向） |
| **多模态机制** | 扩散模型天然多模态 | 锚点先验 + 扩散残差 |
| **计算开销** | 高（完整去噪链梯度 + Critic） | 中（去噪链梯度，无Critic） |
| **优势裁剪** | 对称clip（±ε） | 非对称截断（正保留，负置0） |
| **应用领域** | 机器人操控 | 端到端自动驾驶 |
| **数据集** | Robomimic, Furniture-Bench | NAVSIM v1/v2 |
| **通用性** | 强（通用框架） | 领域特化（锚点依赖驾驶场景特性） |

---

### 3.2 加权回归微调：DIPOLE 与 HDP

DIPOLE 和 HDP 共享完全相同的数学基础——**KL正则化RL的闭式解**，将策略优化转化为加权扩散损失。两者的差异在于**权重函数设计、模型结构和应用侧重**。

#### 共同数学基础：KL正则化RL → 加权回归

KL正则化RL目标有闭式解，最优策略 = 参考策略 × 价值权重：

$$\pi^*(a|s) \propto \mu(a|s) \cdot \exp(\beta \cdot G(s,a))$$

在扩散策略的参数化下，这个闭式解等价于**对扩散损失按价值加权**——不需要通过扩散链回传梯度，仅改变每个训练样本的权重：

$$L_{RL} = \mathbb{E}_{t, \epsilon, s, a}\left[w(s,a) \cdot ||\epsilon - \epsilon_\theta||^2\right], \quad w(s,a) \propto \exp(\beta \cdot G(s,a))$$

核心优势：
- **计算开销与IL几乎相同**（仅多了权重乘法），远低于策略梯度方法（需存储去噪步梯度）
- **天然兼容任意扩散/Flow模型**，不改变扩散训练框架
- **数学上严格等价**于KL正则化RL最优策略

---

#### DIPOLE 的独特设计：二分策略 + CFG式推理控制

**核心问题**：$\exp(\beta G)$ 随价值波动剧烈，导致损失数值不稳定。

**解决方案——二分策略分解**：

1. **权重稳定化**：将 $\exp(\beta G)$ 替换为有界的 $\sigma(\beta G)$（sigmoid），损失始终在可控范围

2. **双模型解耦训练**：
   - $\pi^+$：只学高奖励轨迹（正样本），学习目标"什么是好的"
   - $\pi^-$：只学低奖励轨迹（负样本），学习目标"什么是不好的"
   - 两个Flow模型各自独立稳定训练，互不干扰

3. **CFG式推理控制**：
   $$\tilde{\epsilon} = (1+\omega)\epsilon^+_{\theta} - \omega\epsilon^-_{\theta}$$
   - $\omega = 0$：纯正模型，保守安全
   - $\omega > 0$：远离负模型方向，越贪心
   - 连续可调，无需重新训练

**适用场景**：通用RL框架，从机器人到自动驾驶；需要推理时灵活调节行为风格。

---

#### HDP 的独特设计：Hybrid Loss一致性 + 大规模数据驱动

**核心问题**：在真实驾驶中，开环指标好 ≠ 闭环表现好。如何将扩散策略从仿真迁移到真实道路？

**解决方案——三层工程化设计**：

1. **IL阶段：损失空间与轨迹表示优化**
   - 9种prediction×loss组合中，**τ₀-prediction + τ₀-loss** 最优（收敛快、无高频伪影）
   - **Hybrid Loss**：velocity输出（平滑）+ waypoint监督（空间精度），理论证明仍是有效score matching目标
   - 这是闭环性能的关键桥梁：纯waypoint开环好但闭环抖动，纯velocity平滑但空间精度不足

2. **RL阶段：IL-RL损失一致性**
   - RL保持与IL相同的hybrid loss形式（P-范数），仅加权
   - 去掉一致性直接导致RL初期性能崩溃（distribution shift）
   - 多奖励加权（安全 + 跟车 + 车道保持）推进Pareto前沿

3. **数据规模化**（非算法创新，但最关键）
   - 100K帧：严重mode collapse → 20M帧：多模态涌现 → 70M帧：开环+10%，闭环+20%

**适用场景**：大规模工业级自动驾驶，有海量真实驾驶数据，追求稳定闭环部署。

---

#### DIPOLE vs HDP 对比

| 维度 | DIPOLE | HDP |
|------|--------|-----|
| **相同点** | | |
| 数学基础 | KL正则化RL闭式解 → 加权扩散损失 | 完全相同 |
| 梯度路径 | 不通过扩散链（仅重加权样本） | 完全相同 |
| 训练范式 | 行为克隆预训练 → RL微调 | 完全相同 |
| 计算开销 | 低（与IL几乎相同） | 完全相同 |
| **不同点** | | |
| 模型结构 | **两个**独立Flow模型($\pi^+$, $\pi^-$) | **单一**扩散模型 |
| 权重函数 | **Sigmoid**（$\sigma(\beta G)$，天然有界） | **Exp**（$\exp(\beta r)$，依赖hybrid loss防崩溃） |
| 推理控制 | **ω参数连续可调**贪心度 | **固定策略**，不可调 |
| 理论优雅性 | 二分分解 + CFG机制，通用框架 | Hybrid loss + Bregman divergence，领域特化 |
| IL阶段的贡献 | 基础行为先验 | **系统化工程创新**（损失空间 + 轨迹表示 + 数据规模化） |
| 验证规模 | 仿真基准（ExORL/OGbench/NAVSIM） | **真实车辆200km道路测试** |
| 数据需求 | 常规规模 | **7000万真实驾驶帧** |
| 定位 | 通用RL方法论 | 自动驾驶生产系统 |

#### 本质差异总结

两者的差异根源在于**目标不同**：

- **DIPOLE** 追求**方法论通用性**：二分分解是通用trick，适合任何扩散策略RL任务；CFG式推理控制提供了理论优雅的部署灵活性。
- **HDP** 追求**领域极致性能**：针对自动驾驶做了深度工程优化（损失空间、轨迹表示、数据规模）；单一模型+固定策略简单可靠，适合生产环境。

DIPOLE的二分策略理论上可以应用到HDP的框架中（替换exp权重），HDP的hybrid loss和数据规模化经验也可反馈到DIPOLE的自动驾驶应用中。两者互补而非对立。

---

### 3.3 偏好对齐微调：DriveDPO — DPO + 离散锚点策略

DriveDPO 与前五篇IL+RL方法的关键不同在于：**策略不是扩散模型，而是离散分类分布**（softmax over anchor vocabulary）；**RL方法不是PG也不是加权回归，而是DPO（Direct Preference Optimization）**。

#### 策略形式：离散锚点分类

与DiffusionDriveV2的K-Means锚点相似，DriveDPO也用锚点离散化动作空间——但**不做扩散**，策略直接输出N=8192个锚点轨迹上的softmax概率分布：

$$\pi_\theta(a_i) = \text{Softmax}(\text{MLP}(\text{CrossAttn}(a_i, \text{Scene})))$$

- 锚点来自K-Means聚类人类轨迹
- 锚点token与场景token做Cross-Attention融合
- 策略头直接输出N维分类分布
- 最终选择最高概率锚点作为规划轨迹

这简化了扩散方法的复杂去噪过程，但牺牲了扩散模型的天然多模态表达能力。

#### 两阶段流程

- **阶段1（Unified Policy Distillation）**：融合IL+安全分数→统一分布蒸馏
- **阶段2（Safety DPO）**：迭代DPO偏好对齐，强化安全偏好

#### 阶段1：Unified Policy Distillation

将模仿相似度和规则安全分数融合为**单一软目标分布**，通过KL散度监督策略：

1. **模仿相似度**：$Sim(a_i) = \text{Softmax}(-\|a_i - \hat{a}\|^2)$，与人类轨迹的负欧氏距离
2. **安全分数**：$PDMS(a_i)$，通过NAVSIM模拟器前向仿真得到（含碰撞、可行驶区域、进度、TTC、舒适度）
3. **统一分布**（log变换放大不安全轨迹差异）：
   $$p_{unified}(a_i) = \text{Softmax}\left(w_1 \cdot \log Sim(a_i) + w_2 \cdot \log PDMS(a_i)\right)$$
4. **训练目标**：$L_{unified} = KL(p_{unified} \| \pi_\theta)$

**与DiffusionDriveV2的IL阶段的区别**：DriveDPO在IL阶段就融入了安全信号（PDMS），不是纯粹的行为克隆。

#### 阶段2：Safety DPO

将安全优化形式化为**轨迹级偏好对齐**——让策略偏好"人类like且安全"的轨迹，压制"人类like但不安全"的轨迹。

1. **采样**：从当前策略采样K=1024条轨迹
2. **偏好对构建**（关键设计）：
   - $a_w$（chosen）：PDMS最高的轨迹
   - $a_l$（rejected）：离人类轨迹最近但PDMS < τ的不安全轨迹
   - 这种"安全负样本挖掘"精确打击IL的安全盲区
3. **DPO损失**：
   $$L_{DPO} = -\log \sigma\left(\beta \left[\log\frac{\pi_\theta(a_w)}{\pi_{Ref}(a_w)} - \log\frac{\pi_\theta(a_l)}{\pi_{Ref}(a_l)}\right]\right)$$
   - $\pi_{Ref}$ 是阶段1训练得到的参考策略（冻结）
   - 仍需保留 $L_{unified}$ 作为辅助损失防止分布漂移

#### 与其他IL+RL方法的本质区别

| 维度 | 策略梯度类（DPPO/DDV2） | 加权回归类（DIPOLE/HDP） | 偏好对齐类（DriveDPO） |
|------|------------------------|--------------------------|------------------------|
| **RL范式** | 策略梯度（PG/PPO/GRPO） | KL正则化RL → 加权回归 | DPO偏好对齐 |
| **策略类型** | 扩散模型（连续生成） | 扩散/Flow模型（连续生成） | **离散分类**（N选1） |
| **RL信号形式** | 优势估计（需要奖励/价值） | 价值加权（需要标量奖励） | **偏好比较**（只需相对排序） |
| **是否需要奖励函数** | 是（显式奖励值） | 是（显式奖励值） | **否**（只需成对偏好 A > B） |
| **优化粒度** | 每个去噪步 | 完整轨迹样本 | **轨迹级别**（成对比较） |
| **核心优势** | 理论完备，在线改进 | 计算高效，部署友好 | **安全盲区精确打击，训练极简** |

#### 评价

- 数据集：NAVSIM（PDMS 90.0 SOTA）、Bench2Drive（闭环）
- **PDMS 90.0 超越所有模仿学习和score-based方法**
- 优势：DPO直接建模安全偏好，不需要精心设计奖励函数；离散策略简单高效
- 劣势：离散锚点分类限制了输出多样性（8192中选1），极端场景可能无合适锚点；依赖模拟器的PDMS作为偏好信号

---

## 四、两大类综合对比

### 4.1 纯RL vs IL+RL：核心差异

| 维度 | 纯RL | IL + RL |
|------|------|---------|
| 训练效率 | 低，需大量在线交互或依赖离线数据质量 | IL阶段高效（监督学习），RL阶段聚焦改进 |
| 初始性能 | 极差（随机策略） | 接近人类水平（IL先验） |
| 奖励信号依赖性 | 完全依赖奖励设计 | IL提供稠密监督，RL补充稀疏安全约束 |
| 稳定性 | 训练曲线波动大 | IL提供稳定起点，RL微调幅度可控 |
| 探索难度 | 需从零探索动作空间 | 在IL先验附近局部探索 |
| 是否可能超越人类 | 理论上可以（奖励引导） | RL阶段可能超越IL先验 |
| 代表工作 | CarPlanner, DiffusionQL | DPPO, DiffusionDriveV2, DIPOLE, HDP |
| 适合场景 | 无高质量演示数据、仿真环境 | 有大量真实/仿真驾驶数据、需高效部署 |

### 4.2 IL+RL内部三个子类对比

| 维度 | 策略梯度微调（DPPO、DDV2） | 偏好对齐微调（DriveDPO） | 加权回归微调（DIPOLE、HDP） |
|------|---------------------------|--------------------------|------------------------------|
| **核心思想** | 去噪/决策过程即MDP → PG | 偏好对比较 → DPO对齐 | KL正则化RL闭式解 → 加权回归 |
| **策略类型** | 扩散模型（连续生成） | **离散分类**（锚点N选1） | 扩散/Flow模型（连续生成） |
| **RL信号** | 优势估计（需奖励/价值） | **偏好比较**（A > B，无需标量奖励） | 价值加权（需标量奖励） |
| **是否需价值网络** | DPPO需要，DDV2不需要 | 不需要 | 不需要 |
| **梯度路径** | 通过去噪链 | **通过softmax logit**（极短） | 不通过扩散链（仅重加权） |
| **计算开销** | 高~中 | **极低**（单步forward+softmax） | 低（与IL几乎相同） |
| **训练稳定性** | PPO clip / Intra-Anchor隔离 | KL正则化 + 参考策略冻结 | Sigmoid有界 / Hybrid Loss一致性 |
| **主要局限** | 扩散链长，计算重 | 锚点离散化限制多样性 | 依赖参考策略质量 |

> 各子类内部详细差异见：[3.1节](#31-策略梯度微调dppo-与-diffusiondrivev2)（DPPO vs DDV2）、[3.2节](#32-加权回归微调dipole-与-hdp)（DIPOLE vs HDP）、[3.3节](#33-偏好对齐微调drivedpo--dpo--离散锚点策略)（DriveDPO）。

---

## 五、总结

### 5.1 分类总览

```
纯RL（无IL预训练）
├── 在线RL：CarPlanner — PPO + 自回归模型 + 世界模型
└── 离线RL：DiffusionQL — Q-learning + 扩散策略（联合BC+Q训练）

IL + RL（两阶段：IL预训练 → RL微调）
├── 策略梯度微调（梯度通过扩散链）
│   ├── 共同基础：去噪MDP + 策略梯度（DDV2继承自DPPO）
│   ├── DPPO — 标准扩散 + PPO + GAE（通用框架，需价值网络）
│   └── DiffusionDriveV2 — 锚点截断扩散 + GRPO（搜索空间↓，无价值网络）
├── 偏好对齐微调（梯度通过离散logit）
│   └── DriveDPO — 离散锚点分类 + Safety DPO（无需标量奖励，仅需偏好对）
└── 加权回归微调（梯度不通过扩散链）
    ├── 共同基础：KL正则化RL闭式解 → 对扩散损失按价值加权
    ├── DIPOLE — 二分策略（双模型 + Sigmoid权重 + CFG式推理控制）
    └── HDP   — 单一策略（单模型 + Exp权重 + Hybrid Loss一致性 + 数据规模化 + 实车）
```

### 5.2 关键教训

1. **IL+RL两阶段已成为实际部署标准**：7篇中5篇采用此范式，纯RL在大规模驾驶任务中训练效率仍是瓶颈

2. **三种RL微调路线各有适用场景**：
   - 策略梯度（DPPO/DDV2）：理论完备，适合在线交互环境
   - 偏好对齐（DriveDPO）：无需标量奖励，适合安全偏好明确的场景
   - 加权回归（DIPOLE/HDP）：计算最高效，适合大规模实际部署

3. **加权回归路线的实用优势突出**：不需要通过扩散链回传梯度，计算开销与IL几乎相同（DIPOLE、HDP）

4. **数据规模化可能比算法创新更关键**：HDP的核心发现——70M帧使多模态从无到有涌现，远超出常用基准数据集量级

5. **IL与RL阶段的损失函数一致性至关重要**：HDP去掉RL-hybrid loss导致RL初期性能崩溃；DriveDPO保留统一蒸馏损失作为DPO辅助正则；DIPOLE的Flow matching自然保持一致性

6. **扩散并非唯一选择**：DriveDPO用离散锚点分类+DPO在NAVSIM上达到90.0 PDMS的SOTA，说明简单的离散策略配合有效的偏好对齐也能取得极强性能——关键在于如何将安全信号有效注入策略优化

7. **安全信号的注入方式是核心差异**：CarPlanner用奖励函数，DriveDPO用偏好对，HDP用加权回归，DriveDPO的DPO路线不需要精心设计标量奖励函数，仅需"A比B更安全"的相对比较

8. **从仿真到真实的鸿沟**：开环好 ≠ 闭环好（HDP的hybrid loss是关键桥梁），仿真好 ≠ 真实好（仅HDP做真实道路验证）

6. **从仿真到真实的鸿沟**：开环好 ≠ 闭环好（HDP的hybrid loss是关键桥梁），仿真好 ≠ 真实好（仅HDP做真实道路验证）
